def int_input(num):
#def statement declares a function which inputs a number
   if num != type(int()) and num < 1:
    print("This is not a positive integer, try again:")
#utilises the if statement to ensure that data entered which which is not of the integer type or less than 1 are deemed as not a positive integer
#Python Software Foundation. Python Language Reference, version 3.3. Available at http://www.python.org  

   else:
 
 print(num)
num = input('Please enter a positive integer:')
int_input(num)
#if the data entered is an integer and is greater than one

sum_num = 0

#initialises the sum_num variable to 0

if num != 0:
   for i in range(1, num+1): 
    sum_num += i
    print(sum_num)
else:
    exit()
#if number entered is not equal to 0 and between the range of one and that number, the sum of all numbers between one and that number is calculated.
# if not system is exited.
#Python Software Foundation. Python Language Reference, version 3.3. Available at http://www.python.org
